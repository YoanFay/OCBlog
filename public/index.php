<?php// filter input -> INPUT_GETrequire_once('../src/Controller/Homepage.php');$homepage = new Homepage();if (isset($_GET['action']) && $_GET['action'] !== "") {    switch ($_GET['action']) {        case "post";            if (isset($_GET['id']) && $_GET['id'] > 0) {                //Code pour renvoyer à la page d'un seul post            } else {                echo 'Erreur : il manque un identifiant afin de retrouver le post correspondant';                die;            }            break;        case "comment":            break;        default:            echo "Erreur 404 : La page recherché n'existe pas";    }} else {    $homepage->homepage();}